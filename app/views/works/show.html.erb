<h1><%= @work.title %></h1>

<p><%= @work.description %></p>

<ul>
  <li><%= link_to "Edit", edit_work_path(@work) %></li>
  <li><%= link_to "Destroy", work_path(@work), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>